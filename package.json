{
  "name": "pern-stack-lab",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "setup-db": "cross-env hero42881123 psql -U postgres -f server/database/database.sql",
    "server": "node server/server.js",
    "client": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
